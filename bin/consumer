#!/usr/bin/env php
<?php

use App\Consumer;

require dirname(__DIR__) . '/src/bootstrap.php';

while (true) {
    try {
        $app = new Consumer();
        $app->run();
    } catch (\Throwable $e) {
        var_dump($e->getMessage());
        sleep(15);
    }
}
